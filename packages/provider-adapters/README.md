# Provider adapters

Provider-specific visible-DOM integration, beginning with ChatGPT. All selectors
are exercised only against authored static fixtures; no live provider page or
private endpoint is used by the build.
