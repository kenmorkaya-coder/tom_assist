# ORCHESTRATOR REVIEW 10 — WP-27 accepted; WP28-1 ruling; corrective package

**Date:** 1 September 2026

## WP-27 — accepted

Forensics verified and accepted as reported (validation 14 passed; frozen artifacts untouched; H0 stands). Three causal findings supersede all prior hypotheses: (1) the pilot runner never engaged the five-dynamics commit path — trees unloaded, RGMs empty, structural channel inert; (2) ledger retrieval was correct (focus objects 33/33, 207/207 citation objects admitted, zero budget evictions) but the renderer emitted only 3/207 `state_id`s, making exact citation structurally impossible; (3) the product commit path drives the engine from a 3-byte SHA text hash (content noise) and retrieval collapses the channel-separated projection to three axes. The orchestrator's earlier uniform-axis hypothesis is refuted by the data and withdrawn. Codex's WP-28 stop (refusing to compose shadow-only physics without an owner ruling) was correct governance.

## WP28-1 — owner ruling (1 Sep 2026): option (a)

**`agency/mechanics/sicd_msr_load_application.py` is the authoritative product commit-drive surface**: the canonical 17D-to-step plan/application, using `sicd_msr_routing_basis.project_load_signature_to_routing_basis`, feeding the engine's native `semantic_routing_basis` hook. The channel-separated loading-aware readout remains shadow-only upstream, per its own label. No upstream change required.

## Corrective package (WP-29) — authorized

1. **Commit drive:** replace the 3-byte-SHA projection and 2D difference wind with the real 17-channel text projection driving the engine via `sicd_msr_load_application`'s canonical path (pinned imports only; cite file:line). Teaching, rotation, re-seat, purity, checkpoints unchanged.
2. **Retrieval coherence:** make the structural channel's cohort selection consistent with the adopted routing-basis drive using pinned pure functions only (routing-basis-projected query against branch `sem_vec` in 8D; anchors via `rank_by_branch_resonance` as today). If any composition would require inventing physics, STOP and escalate — never approximate.
3. **Renderer answerability:** packet lines carry their `state_id`s; golden updates are expected and legitimate here (policy version bump; document).
4. **Battery runner realism:** case histories are committed through the real five-dynamics path before the probe (authored assistant turns teach; zero provider calls during import).
5. **Reporting fixes:** cluster-bootstrap pairing overwrite; Prompt/Response SHA labels hash raw UTF-8; rename the `capture/lineage mismatch` taxonomy label to what it measures (citation-list inequality).
6. **Pre-registration v2 (DRAFT-PENDING-OWNER-FREEZE):** multiset (order-insensitive) citation comparison with rationale; rendered-ID answerability noted as fixed; margins, H-slots, selection, seed otherwise unchanged. No live generations — pilot v3 requires the v2 freeze and fresh owner authorization.

Evidence in BUILD_LOG; stop for audit. Frozen v2 pilot artifacts remain permanent; no G-gate verdicts.
