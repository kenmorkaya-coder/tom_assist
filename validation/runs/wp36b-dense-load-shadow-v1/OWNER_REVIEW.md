# WP-36b owner review

**SHADOW CALIBRATION — NOT A GATE**

The construction produced 17 strictly positive, replayable q17 values and three
strictly positive, separate T/S/P values in all 27 observations. The intended
combined channel ranked first in 14/17 channel cases; the misses were frequency,
persistence and burstiness because the same stored history also produced a
stronger recurrence observation. This is useful evidence that those four
dynamics remain coupled and must not be declared calibrated yet.

Semantic projection alone placed the intended channel in the top three for
15/17 cases. The misses were contradiction (rank 5) and inference (rank 14);
their exact validated structural evidence made the combined channel rank first.
This means MiniLM resonance is a continuous density measurement, not a safe
replacement for the structural parser.

Both paraphrase pairs retained their intended combined winner. The directed
forward/reversal graph fingerprints and final digests differed, while their q17
vectors remained very close (cosine 0.9999989). Direction is therefore preserved
by the entity-addressed graph, not falsely claimed as recoverable from scalar
magnitudes alone. The five-chunk tail case selected the final load-bearing chunk.

The pinned Python `agency.mechanics.sicd_engine.TreeGrowthEngine` loaded the
`msr_8d_native_10k` seed with 10,000 branches and applied q17/8D/TSP shapes of
17/8/3 to a disposable clone. Tick advanced 4707→4708 and branches 10000→10004.
The original runtime bytes remained exactly unchanged. The pre-registered exact
routing-basis comparison was false: the engine normalizes the already normalized
plan again, producing a maximum float delta of 1.1102230246251565e-16. The
frozen false result is retained; `DIAGNOSTIC.json` records the zero-generation
cause rather than weakening the expectation.

Conclusion: the mechanics path is genuinely the pinned non-Rust Python 10K tree,
multi-chunk semantic density and independent T/S/P are technically viable, and
the non-zero rule is met without a floor. The result is not ready for production:
three history channels need disentangling, semantic contradiction/inference need
structural corroboration, and the exact double-normalization discrepancy remains
visible. No Feeling Wheel or provider generation was used.
